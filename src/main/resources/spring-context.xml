<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.1.xsd
      http://www.springframework.org/schema/mvc
      http://www.springframework.org/schema/mvc/spring-mvc-4.1.xsd">

    <mvc:annotation-driven/>
    <context:component-scan base-package="ru.sstu.ushankashop"/>

    <!--прописываем настройки из db.properties в dataSource (db connection-pool HikariCP)-->
    <bean class="com.zaxxer.hikari.HikariDataSource" id="dataSource">
        <property name="jdbcUrl" value="${url}"/>
        <property name="username" value="${user}"/>
        <property name="password" value="${password}"/>
        <property name="driverClassName" value="${driver}"/>
    </bean>
    <!--прописываем настройки из db.properties в dataSource (driver manager spring-jdbc)-->
    <!--<bean class="org.springframework.jdbc.datasource.DriverManagerDataSource" id="dataSource">-->
    <!--<property name="url" value="${url}"/>-->
    <!--<property name="username" value="${user}"/>-->
    <!--<property name="password" value="${password}"/>-->
    <!--<property name="driverClassName" value="${driver}"/>-->
    <!--</bean>-->

    <!--подключаем проперти-файл db.properties - настройки mysql-->
    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:db.properties</value>
            </list>
        </property>
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE"/>
    </bean>

    <!--создаем бин для работы с базой данных (spring-jdbc) и передаем ему dataSource-->
    <!--<bean class="org.springframework.jdbc.core.JdbcTemplate" id="jdbcTemplate">-->
        <!--<constructor-arg ref="dataSource"/>-->
    <!--</bean>-->
</beans>